{
  "scripts": {
    "dev": "parcel index.html --open",
    "dev:curl": "parcel curl.html --open",
    "dev:trail": "parcel trail.html --open",
    "build": "parcel build index.html"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "parcel-bundler": "^1.12.3"
  },
  "dependencies": {
    "bezier-easing": "^2.1.0",
    "glsl-curl-noise": "^0.0.4",
    "glsl-easings": "^1.0.0",
    "glsl-fast-gaussian-blur": "^1.0.2",
    "glsl-noise": "^0.0.0",
    "glsl-random": "^0.0.5",
    "scripts": "^0.1.0"
  }
}
