{
  "scripts": {
    "dev": "parcel smoke.html --open",
    "build": "parcel build smoke.html"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "parcel-bundler": "^1.12.3"
  },
  "dependencies": {
    "bezier-easing": "^2.1.0",
    "glsl-noise": "^0.0.0",
    "scripts": "^0.1.0"
  }
}
